export const styles = theme => ({
  root: {
    display: 'flex',
    flexWrap: 'wrap',
  },
  formControl: {
    margin: theme.spacing.unit,
    width: "100%",
  },
  noLabel: {
    marginTop: theme.spacing.unit * 1,
  },
});
